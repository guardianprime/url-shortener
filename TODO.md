## Production-level enhancement (optional)

If you want to be extra clean:

Don’t store QR images at all

Generate them dynamically with caching headers

Or store only when user explicitly saves/downloads
